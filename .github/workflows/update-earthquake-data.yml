name: Update Earthquake Data

on: 
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
    inputs:
      deploy:
        description: 'Deploy'
        required: true
        default: 'Deploy'
        type: choice
        options:
        - Deploy
  
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy
        uses: wei/curl@v1
        with:
          args: -X POST ${{secrets.DEPLOY_URL}}
